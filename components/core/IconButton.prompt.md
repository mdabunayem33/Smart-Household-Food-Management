Round 48px+ icon-only tap target. Requires Material Symbols Rounded font linked (see readme Iconography).

```jsx
<IconButton icon="arrow_back" variant="ghost" ariaLabel="Back" />
<IconButton icon="edit" variant="surface" />
<IconButton icon="add" variant="filled" size="lg" />
```
