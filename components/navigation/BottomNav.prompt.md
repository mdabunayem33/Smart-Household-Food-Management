Fixed 5-tab bottom nav — Home / Inventory / Shopping / Analytics / Profile. Blurred translucent
background so scrolled content doesn't clip abruptly.

```jsx
<BottomNav active="home" onChange={(key) => {}} />
```
