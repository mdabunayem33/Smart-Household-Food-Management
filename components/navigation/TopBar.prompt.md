Simple centered-title bar with optional back button and trailing action — used on every
non-tab-root screen (Add Food steps, Food Detail, Profile sub-pages).

```jsx
<TopBar title="Choose a category" onBack={() => {}} />
```
