Filled, no-border text input. Used sparingly — this app is "no typing" by design wherever a picker
will do (categories, units, quantities); reserve for Profile fields, family member names, search.

```jsx
<TextField label="Family member name" placeholder="e.g. Grandma Rosa" icon="person" />
```
